/// All `login` packets changed at least once since protocol version 47.
pub mod login;
/// All `play` packets changed at least once since protocol version 47.
pub mod play;

/// All types changed at least once since protocol version 47.
pub mod types;
